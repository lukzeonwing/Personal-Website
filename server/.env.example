# Server Configuration
PORT=4000

# Domain Configuration (change this when deploying to your domain)
# Example: DOMAIN=mysite.com or DOMAIN=mysite.example.com
# Leave empty for localhost-only development
DOMAIN=

# Security - REQUIRED (generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
JWT_SECRET=your-secure-jwt-secret-min-32-chars
ADMIN_USERNAME=your-admin-username
ADMIN_PASSWORD=your-secure-password-min-8-chars

# CORS - Comma-separated list of allowed origins
# localhost origins are always included, domain origins are auto-added if DOMAIN is set
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:4000

# File Upload Limits
BODY_SIZE_LIMIT=50mb
